---
title: 固件
description: 给控制板刷上 FluidNC——一条命令（`physiclaw flash`）或手动浏览器流程——以及它会加载的配置文件。
---

PhysiClaw 硬件通过 [FluidNC](https://github.com/bdring/FluidNC) 固件与电脑通信：电脑用 [G-code](https://en.wikipedia.org/wiki/G-code)（GRBL 协议）发送指令，控制电机转动和电磁铁开关，完成点击和滑动等手势。

## 准备工作

- [MKS DLC32 V2.1](https://github.com/makerbase-mks/mks-dlc32) 控制板。
- USB-B 转 USB-A 数据线。
- 12 V 2 A 电源。
- Chrome 浏览器（仅下面的手动方式需要）。

你可以用一条命令烧录 FluidNC，也可以通过 FluidNC 网页安装器手动烧录。

## 命令行烧录（推荐）

通过 USB 连接控制板（接通 12 V 电源、**断开步进电机**），然后运行：

```bash
physiclaw flash
```

### 验证

连接两个电机和电磁铁，手动测试一遍，确认一切正常：

```bash
physiclaw testdrive
```

<svg viewBox="0 0 340 330" role="img" aria-label="俯视机器：原点在左上角，X 沿电机横梁向右，Y 远离横梁" style="max-width:340px;width:100%;height:auto;margin:1.5rem auto;display:block" xmlns="http://www.w3.org/2000/svg" font-family="'Inter','Segoe UI',system-ui,sans-serif">
  <g fill="none" stroke-linecap="round" stroke-linejoin="round">
    <path d="M 62 51 Q 180 48.5 298 51 Q 300.5 170 298.5 288 Q 180 290.5 61.5 288.5 Q 59.5 170 62 51 Z" stroke="currentColor" stroke-width="2"/>
    <path d="M 89 57.5 Q 106 55.5 123 57 Q 125 74 123.5 90.5 Q 106 92.5 89.5 91 Q 87.5 74 89 57.5 Z" stroke="currentColor" stroke-width="1.5"/>
    <text x="106" y="80" text-anchor="middle" font-size="15" font-weight="600" fill="currentColor" stroke="none">A</text>
    <path d="M 237.5 57 Q 254 55.5 271 57.5 Q 272.5 74 271 91 Q 254 92.5 237 90.5 Q 235.5 74 237.5 57 Z" stroke="currentColor" stroke-width="1.5"/>
    <text x="254" y="80" text-anchor="middle" font-size="15" font-weight="600" fill="currentColor" stroke="none">B</text>
    <path d="M 149 111 Q 180 109.5 211 110.5 Q 224 111 224.5 124 Q 226 190 224.5 256 Q 224 269 211 269.5 Q 180 271 149.5 269.5 Q 136 269 135.5 256 Q 134.5 190 136 124 Q 136.5 111.5 149 111 Z" stroke="currentColor" stroke-width="1.5" opacity="0.7"/>
    <path d="M 165 256 Q 180 257 195 256" stroke="currentColor" stroke-width="3" opacity="0.5"/>
    <circle cx="40" cy="30" r="5" fill="#3b82f6" stroke="none"/>
    <path d="M 48 30 Q 117 28.5 188 30 M 179 24 L 189 30 L 179 36" stroke="#3b82f6" stroke-width="2"/>
    <text x="200" y="35" font-size="14" font-weight="600" fill="#3b82f6" stroke="none">X+</text>
    <path d="M 40 38 Q 38.5 119 40 200 M 34 191 L 40 201 L 46 191" stroke="#3b82f6" stroke-width="2"/>
    <text x="40" y="226" text-anchor="middle" font-size="14" font-weight="600" fill="#3b82f6" stroke="none">Y+</text>
  </g>
</svg>

试试 `X10` / `Y10`，确认每次移动都与上图坐标轴的方向一致；再用 `tap` 检查电磁铁是否正常。

:::tip
电机 A 接主板的 Y1 口，电机 B 接 X 口，电磁铁接主轴（spindle）端子。
:::

## 手动烧录

若 `physiclaw flash` 执行成功，可跳过这部分。

<details>
<summary>手动烧录流程</summary>

<Steps>

1. **给控制板上电。** 接 **12 V** 电源，用 **USB** 串口线连接到电脑。

2. **打开安装器。** 在 Chrome 里打开 [FluidNC 网页安装器](https://installer.fluidnc.com)。页面会显示两个产品——在 **FluidNC** 下点击 **Continue**。

3. **连接控制板。** 点击 **Connect**。弹出的对话框会列出可用串口。选 **USB Serial**，点击 **Connect**。

4. **进入 Install。** 出现 Home 页。点击左侧栏的 **Install**。

5. **选择固件变体。** 从下拉里选最新版本。会出现三个变体，选择 **`noradio`**。

6. **选择安装类型。** 选择 **fresh-install**。

7. **确认。** 弹出确认框。确保 **Installation speed** 是 **115200 baud**，然后点击 **Install**。

8. **等待。** 出现进度条，等待完成。

9. **安装完成。** 一个 "Done" 对话框确认完成。点击 **Continue**。

10. **打开 File browser。** Home 页会显示红色的 **"invalid configuration"** 警告，这是因为控制板已经刷入固件，但还没有配置。点击左侧栏的 **File browser**。

11. **创建配置文件。** File browser 会提示你创建一个。点击 **Create config**；弹出的对话框里 `config.yaml` 已预填为文件名。点击 **OK**。

12. **打开 Source 标签页。** 编辑器停在 **General** 标签。点击最右侧的 **Source** 标签。

13. **粘贴配置。** 全选已有文本，删掉，再把下面[配置文件](#配置文件)一节的**全部内容**粘进去。

14. **保存。** 点击 **Save**。

15. **进入 Terminal。** 点击左侧栏的 **Terminal**。

16. **重启控制板。** 点击红色的 **Restart** 按钮。控制板重启并打印启动日志。

17. **验证。** 每条消息都应当是**绿色**的，最后一行应为 `<Idle|...>`——说明刷入固件并配置成功。

</Steps>

</details>

## 配置文件

命令行烧录会自动帮你刷入；手动烧录则需在第 13 步粘贴。FluidNC 配置：

<details>
<summary>config.yaml</summary>

```yaml
# FluidNC config — MKS DLC32 v2.1 — CoreXY + Solenoid
# Comments ONLY on their own lines (FluidNC YAML subset rule)
# Tune: steps_per_mm, max_travel_mm, acceleration, direction pins

board: MKS-DLC32 V2.1
name: PhysiClaw

# I2S bus — hardwired on DLC32 v2.1, do not change
i2so:
  bck_pin: gpio.16
  data_pin: gpio.21
  ws_pin: gpio.17

# I2S_STATIC only — I2S_STREAM causes drift on this board
stepping:
  engine: I2S_STATIC
  idle_ms: 250
  pulse_us: 4
  dir_delay_us: 1
  disable_delay_us: 0

kinematics:
  corexy:

axes:
  shared_stepper_disable_pin: I2SO.0

  x:
    # Tune: (motor_steps x microsteps) / mm_per_rev
    # e.g. 200 x 16 / 40mm (20T GT2) = 80
    steps_per_mm: 80.000
    max_rate_mm_per_min: 10000.000
    acceleration_mm_per_sec2: 1000.000
    # Set max_travel_mm to your actual X travel
    max_travel_mm: 200.000
    soft_limits: false

    motor0:
      hard_limits: false
      stepstick:
        step_pin: I2SO.1
        # Add :low after I2SO.2 if X runs backwards
        direction_pin: I2SO.2:low

  y:
    steps_per_mm: 80.000
    max_rate_mm_per_min: 10000.000
    acceleration_mm_per_sec2: 1000.000
    # Set max_travel_mm to your actual Y travel
    max_travel_mm: 200.000
    soft_limits: false

    motor0:
      hard_limits: false
      stepstick:
        step_pin: I2SO.5
        # Add :low after I2SO.6 if Y runs backwards
        direction_pin: I2SO.6:low

PWM:
  pwm_hz: 1000
  output_pin: gpio.32
  enable_pin: NO_PIN
  direction_pin: NO_PIN
  disable_with_s0: false
  s0_with_disable: true
  spinup_ms: 0
  spindown_ms: 0
  tool_num: 0
  speed_map: 0=0% 1000=100%
  off_on_alarm: true

start:
  must_home: false
```

</details>
